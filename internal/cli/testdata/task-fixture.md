# TASK-FIXTURE

> Primary contract: CONTRACTS-FIXTURE

This is a synthetic task used solely by the CLI runtime tests. It is not a
real task and never enters the release tarball (the Go `testdata` directory is
excluded from packaging).
