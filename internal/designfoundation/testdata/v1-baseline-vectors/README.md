# v1.0 baseline vectors

Captured before I1 overwrite. Hashes in parent dir. Compare with `sha256sum docs/design/*-template.md`.

Samples:
- local-minimal: Foundation reference = local, no global DESIGN.md published entry
- core-minimal: single published DESIGN.md with one marker
- extended-minimal: two surfaces
