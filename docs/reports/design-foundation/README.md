# Design Foundation reports

This factory ships one template:

| File | When to fill |
|:---|:---|
| `FOUNDATION-REPLAY-template.md` | After a **real product** has two `UI impact=changed` REQs. Never in this repository. |

Method conclusions live in `docs/rules/design-foundation.md` and `skills/design-foundation`. Do not check in trial HTML, filled Kernels, or dated investigation dumps here — they would install into every target project.
