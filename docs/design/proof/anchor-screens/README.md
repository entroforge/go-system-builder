# Anchor and stress screens

Place selected HTML proofs here after F5. Use the four-field prototype header from `docs/rules/ui-prototype.md`. These files prove Kernel/Grammar; they do not replace `docs/design/prototypes/<module>/`.
